apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_example.fullname" . }}
  labels:
    {{- include "helm_example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm_example.selectorLabels" . | nindent 4 }}
